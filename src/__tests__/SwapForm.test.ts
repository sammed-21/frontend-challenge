import { describe, it } from 'vitest'

describe('SwapForm', () => {
  it.todo('should disable submit when no tokens are selected')
  it.todo('should disable submit when amount is empty')
  it.todo('should show quote data when available')
  it.todo('should call addOrder with correct OrderIntent on submit')
})
